#include<stdio.h>
int main()
{
    int size,i,j;
    printf("enter size of your pattern: ");
    scanf("%d",&size);
    for(i=1;i<=size;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("%d ",);
        }
        printf("\n");
    }
    return 0;
}